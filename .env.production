# Variables de entorno para producción
# IMPORTANTE: Nunca subir este archivo con valores reales a git

# ==============================================
# CONFIGURACIONES OBLIGATORIAS DE SEGURIDAD
# ==============================================

# SECRET_KEY: Genera una nueva clave secreta única de al menos 50 caracteres
# Usa: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=GENERA-UNA-CLAVE-SUPER-SECRETA-AQUI-DE-50-CARACTERES-MINIMO

# DEBUG: SIEMPRE False en producción
DEBUG=False

# ALLOWED_HOSTS: Dominios permitidos (separados por comas, sin espacios)
ALLOWED_HOSTS=tudominio.com,www.tudominio.com,tu-ip-del-servidor

# ==============================================
# BASE DE DATOS (PostgreSQL recomendado)
# ==============================================

# Para PostgreSQL
DATABASE_ENGINE=django.db.backends.postgresql
DATABASE_NAME=dulce_bias_db
DATABASE_USER=dulce_bias_user
DATABASE_PASSWORD=contraseña-super-segura-123
DATABASE_HOST=localhost
DATABASE_PORT=5432

# Para mantener SQLite (solo para proyectos pequeños)
# DATABASE_ENGINE=django.db.backends.sqlite3
# DATABASE_NAME=db.sqlite3

# ==============================================
# CONFIGURACIONES DE EMAIL
# ==============================================

# Gmail (más común)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu-email@gmail.com
EMAIL_HOST_PASSWORD=tu-app-password-de-gmail
DEFAULT_FROM_EMAIL=noreply@tudominio.com

# ==============================================
# SEGURIDAD AVANZADA
# ==============================================

# IPs permitidas para acceso al admin (separadas por comas)
ADMIN_IP_WHITELIST=tu-ip-casa,ip-oficina,ip-trabajo

# Configuraciones SSL (para HTTPS)
SECURE_SSL_REDIRECT=True
SECURE_PROXY_SSL_HEADER=HTTP_X_FORWARDED_PROTO,https
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True

# Rate Limiting (límites de requests)
RATE_LIMIT_LOGIN=5/m
RATE_LIMIT_PASSWORD_RESET=3/h
RATE_LIMIT_CONTACT_FORM=10/h

# Configuración de Axes (protección contra ataques)
AXES_FAILURE_LIMIT=5
AXES_COOLOFF_TIME=1

# ==============================================
# ARCHIVOS ESTÁTICOS Y MEDIA (Opcional)
# ==============================================

# Para usar AWS S3 (recomendado para producción)
# USE_S3=True
# AWS_ACCESS_KEY_ID=tu-access-key
# AWS_SECRET_ACCESS_KEY=tu-secret-key
# AWS_STORAGE_BUCKET_NAME=dulce-bias-static
# AWS_S3_REGION_NAME=us-east-1

# Para usar archivos locales (más simple)
USE_S3=False
STATIC_ROOT=/var/www/dulce-bias/static/
MEDIA_ROOT=/var/www/dulce-bias/media/

# ==============================================
# CACHE Y REDIS (Opcional para mejor rendimiento)
# ==============================================

# Si tienes Redis instalado
# CACHE_BACKEND=django_redis.cache.RedisCache
# CACHE_LOCATION=redis://127.0.0.1:6379/1

# Cache en memoria (por defecto)
CACHE_BACKEND=django.core.cache.backends.locmem.LocMemCache

# ==============================================
# CONFIGURACIONES ESPECÍFICAS DEL NEGOCIO
# ==============================================

# Nombre de la empresa
COMPANY_NAME=Galletas Kati
COMPANY_EMAIL=contacto@galletaskati.com
COMPANY_PHONE=+56912345678

# Configuraciones de envío
DEFAULT_SHIPPING_COST=2990
FREE_SHIPPING_THRESHOLD=25000

# ==============================================
# LOGS Y MONITOREO
# ==============================================

# Nivel de logging en producción
LOG_LEVEL=WARNING

# Directorio para logs
LOG_DIR=/var/log/dulce-bias/

# ==============================================
# INSTRUCCIONES DE USO:
# ==============================================

# 1. Copia este archivo como .env en tu servidor
# 2. Reemplaza TODOS los valores con datos reales
# 3. Genera una SECRET_KEY nueva con:
#    python -c "import secrets; print(secrets.token_urlsafe(50))"
# 4. Configura tu dominio real en ALLOWED_HOSTS
# 5. Configura email real para notificaciones
# 6. NUNCA subas el archivo .env real a git
# 7. Asegúrate de que .env esté en .gitignore
